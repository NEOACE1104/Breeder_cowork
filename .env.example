# Backend Configuration
# For production, you might want to use PostgreSQL or another robust database.
DATABASE_URL=sqlite+aiosqlite:///./app.db

# CORS origins, comma-separated for multiple origins
# Example: CORS_ORIGINS=http://localhost:5173,https://your-production-domain.com
CORS_ORIGINS=http://localhost:5173

# A strong, randomly generated secret key is crucial for security.
# You can generate one with: openssl rand -hex 32
SECRET_KEY=a-very-secret-key-that-you-should-change

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Maximum file upload size in megabytes
MAX_UPLOAD_MB=200
